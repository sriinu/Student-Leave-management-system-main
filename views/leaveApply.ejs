<% include ./partial/header %> <% include ./partial/studfunction %>

<div
  class="col-sm-6"
  style="    flex: 0 0 50%;
max-width: 50%;
margin-left: 431px;
margin-top: -181px;  text-align: -webkit-center;"
>
  <h1>Apply for leave here <%=student.name%></h1>
  <form action="/student/apply/<%=student._id%>" method="POST">
    <div class="form-group">
      <input
        type="text"
        placeholder="subject"
        name="subject"
        class="form-control"
        required
      />
    </div>
    <div class="form-group">
      <label for="from">From:</label>
      <input
        type="date"
        data-date-inline-picker="true"
        id="from"
        name="from"
        required
      />
    </div>
    <div class="form-group">
      <label for="to">To:</label>
      <input
        type="date"
        data-date-inline-picker="true"
        id="to"
        name="to"
        required
      />
    </div>

    <div class="form-group">
      <button class="btn btn-lg btn-block btn-default">Submit</button>
    </div>
  </form>

  <% include ./partial/footer %>
</div>
